package com.priyakdey.arrays;

/**
 * @author Priyak Dey
 */
public class KadaneAlgorithm {

    long maxSubarraySum(int arr[], int n) {
        // incase the len == 1, we return the element as sum
        if (n == 1) return arr[0];

        int start = 0, end = 0;
        int max = Integer.MIN_VALUE;


    }

}
